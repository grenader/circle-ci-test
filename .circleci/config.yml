executors:
  j11:
    docker:
      - image: 'cimg/openjdk:11.0'
orbs:
  gradle: circleci/gradle@2.2.0
version: 2.1
workflows:
  checkout-build-test:
    jobs:
      - gradle/test:
          executor: j11
